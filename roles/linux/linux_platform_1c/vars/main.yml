---
    platform: "{{ setup_full['files'] | map(attribute='path') | join('') | regex_replace('\\/tmp\\/', '') }}"
    version: "{{ setup_full['files'] | map(attribute='path') | join('') | regex_replace('\\/tmp\\/', '') | regex_search('8\\.3\\.([0-9]{2})\\.([0-9]{4})') }}"
    env_path_rac: /opt/1cv8/bin
    logcfg_path: /opt/1cv8/conf/logcfg.xml
